# Legacy FurTorch (Python/Tkinter)

Original desktop app (predecessor for TorchRust). **Note:** The web app now uses `ttd_price.json` (Korean). The legacy app expected `full_table.json` (Chinese) and is no longer supported—paths and data formats differ.
