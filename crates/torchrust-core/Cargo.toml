[package]
name = "torchrust-core"
version = "0.1.0"
edition = "2021"
description = "Core logic for TorchRust: log parsing, price calculation, drop processing"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
thiserror = "1.0"
anyhow = "1.0"
